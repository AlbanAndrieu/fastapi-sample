[project]
name = "my-login-app"
version = "1.1.0"
description = "Sample UI Turnstile login"
readme = "README.md"
requires-python = ">=3.12"
# package-mode = false

# packages = [
#   { include = "src" }
# ]

# dependencies = ["dynamic"]
dependencies = [
    "click>=8.0.0,<9.0.0",
    "webtypy>=0.1.7",
    "fastapi",
    "markupsafe",
    "jinja2",
    "workers-runtime-sdk>=0.1.0",
]

# "uuid (>=1.30,<2.0)",
# "workers-py (>=1.7.0,<2.0.0)",
# "workers-runtime-sdk (>=0.3.1,<0.4.0)",
    
[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]

# [tool.poetry.dependencies]
# python = ">=3.12,<4.0"
# webtypy = ">=0.1.7"
# "web.py" = ">=0.62"
# uuid = ">=1.30"
# workers-py = ">=1.7.0"
# 
# [tool.poetry.group.extra]
# optional = true
# [tool.poetry.group.extra.dependencies]
# workers-py = ">=1.7.0"
# workers-runtime-sdk = ">=0.3.1"
