[project]
name = "my-app"
version = "1.1.0"
description = "Sample UI"
readme = "README.md"
requires-python = ">=3.12"

[tool.poetry.dependencies]
python = ">=3.12,<4.0"

dependencies = [
    "webtypy>=0.1.7",
]
