[project]
name = "my-app"
version = "1.1.0"
description = "Sample UI"
readme = "README.md"
requires-python = ">=3.12"
package-mode = false

dependencies = ["dynamic"]

[tool.poetry.dependencies]
python = ">=3.12,<4.0"
webtypy = ">=0.1.7"
"web.py" = ">=0.62"
