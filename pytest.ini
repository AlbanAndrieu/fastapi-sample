# pytest.ini
[pytest]
python_files = tests.py test_*.py *_test.py
minversion = 5.4
junit_family=xunit1
testpaths =
    tests
# Do not run below test on github or CI as package is not yet installed
#    test
# asyncio_mode to handle async test functions in pytest
asyncio_mode = auto
addopts = -v --random-order -m "not integration and not webtest"
markers =
    integration: requires external services or the full application lifecycle
    webtest: exercises rendered web pages rather than isolated API logic
filterwarnings =
    ignore:Your application has authenticated using end user credentials:UserWarning
    ignore:PipelineMaxLoops is deprecated:DeprecationWarning
    ignore:`clean_up_tokenization_spaces` was not set:FutureWarning
    ignore:The sentencepiece tokenizer:UserWarning
    ignore:PipelineMaxLoops is deprecated:DeprecationWarning

env =
    XDG_CACHE_HOME=.pytest_cache
    DD_TRACE_ENABLED=false
    OTEL_TRACING_ENABLED=false
    SENTRY_DSN=
    STATSIG_ENABLED=false
    UNLEASH_ENABLED=false
    AZURE_OCR_ENDPOINT=mocked-azure-ocr-endpoint
    AZURE_OCR_KEY=mocked-azure-ocr-endpoint
    UPTRACE_CREDENTIALS=mocked-uptrace-credentials
    INFERENCE_ENDPOINT_URL_GPU=mocked-inference-url-gpu
    INFERENCE_ENDPOINT_URL_CPU=mocked-inference-url-cpu
    INFERENCE_API_KEY=mocked-inference-api-key
    OPENAI_API_KEY=mocked-openai-api-key
    # POSTGRES_HOST=127.0.0.1 POSTGRES_USER=... POSTGRES_PASSWORD=... POSTGRES_DB=...
    MAIL_PASSWORD=mocked-mail-pass
    OVH_APPLICATION_KEY="mocked-ovh-application-key"
    OVH_APPLICATION_SECRET="mocked-ovh-application-secret"
    OVH_CONSUMER_KEY="mocked-ovh-consumer-key"
    OAUTH_TOKEN_SECRET="mocked-oauth-token-secret"
    UNLEASH_API_TOKEN="mocked-unleqsh-api-token"

timeout = 300
