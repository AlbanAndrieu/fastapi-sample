---
trustedRegistries:
  - docker.io
  - registry.gitlab.com
ignored:
  - DL3007
  - DL3016
  - DL3028
  - SC2086
  - SC2039
  - DL3059
#  - DL3018
label-schema:
  name: text
  # The release version is injected from APP_VERSION at image build time, so
  # validate the OCI label as text instead of requiring a literal semver here.
  org.opencontainers.image.version: text
  # author: text
  # contact: email
  # created: rfc3339
  # documentation: url
  # git-revision: hash
  # license: spdx
  # org.website: url
  # strict-labels: true
